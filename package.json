{
  "name": "blog-fazeela-nizam",
  "productName": "Blog | Fazeela Nizam",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "export": "next export",
    "deploy": "del node_modules\\.cache && next build && next export && echo > out/.nojekyll && git add out/ && git commit -m \"Deploy Next.js to gh-pages\" && git subtree push --prefix out origin gh-pages"

  },
  "dependencies": {
    "gray-matter": "^4.0.2",
    "marked": "^1.2.8",
    "next": "^10.0.0",
    "prismjs": "^1.23.0",
    "raw-loader": "^4.0.2",
    "react": "16.13.1",
    "react-dom": "16.13.1"
  }
}
